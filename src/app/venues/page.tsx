'use client';
import { useEffect,useState } from 'react'; import { Nav } from '@/components/Nav'; import { supabase } from '@/lib/supabase';
type VenueRow={venue_name:string;visits:number;average:number};
export default function Venues(){const [rows,setRows]=useState<VenueRow[]>([]);useEffect(()=>{supabase.from('venue_stats').select('*').order('visits',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <><Nav/><main className="shell"><section className="hero"><div><div className="eyebrow">// rooms that made your ears ring</div><h1>VENUE <span className="accent">RATINGS</span></h1></div></section><div className="grid">{rows.map(v=><article className="card" key={v.venue_name}><h2 style={{fontFamily:'Anton'}}>{v.venue_name}</h2><div className="rating" style={{font:'28px Anton',color:'var(--pink)'}}>{Number(v.average).toFixed(1)}/5</div><p className="meta">{v.visits} logged visit{v.visits===1?'':'s'}</p></article>)}</div></main></>}
